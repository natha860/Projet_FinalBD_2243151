@model IEnumerable<BD_Final_Project.ViewModel.ImageVM>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Joueurs.Nom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Joueurs.DateNaissance)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Joueurs.Position)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Joueurs.Nationalite)
            </th>
        
            <th>
                @Html.DisplayNameFor(model => model.Joueurs.Equipe)
            </th>
            <th>
                image
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                    @Html.DisplayFor(modelItem => item.Joueurs.Nom)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.Joueurs.DateNaissance)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.Joueurs.Position)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.Joueurs.Nationalite)
            </td>
           
            <td>
                    @Html.DisplayFor(modelItem => item.Joueurs.Equipe.Nom)
            </td>
                <td>
                    <img alt="@(item.Joueurs.Nom)" src="@(item.fichierImage)" width="200" height="200" />
                </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.Joueurs.JoueurId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Joueurs.JoueurId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Joueurs.JoueurId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
